<Application
    x:Class="HuaweiWmiControl.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HuaweiWmiControl"
    xmlns:controls="using:HuaweiWmiControl.Controls">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- FeatureCard 默认样式 -->
            <Style TargetType="controls:FeatureCard">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="controls:FeatureCard">
                            <StackPanel Spacing="16">
                                <TextBlock Text="{TemplateBinding Title}"
                                           Style="{StaticResource TitleTextBlockStyle}"/>
                                <TextBlock Text="{TemplateBinding Description}"
                                           Style="{StaticResource BodyTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1" CornerRadius="8">
                                    <ContentPresenter Content="{TemplateBinding Content}"
                                                      Margin="{TemplateBinding InnerPadding}"/>
                                </Border>
                            </StackPanel>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
